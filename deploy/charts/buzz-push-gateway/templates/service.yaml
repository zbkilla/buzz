apiVersion: v1
kind: Service
metadata:
  name: {{ include "push.name" . }}
  labels: {{- include "push.runtimeLabels" . | nindent 4 }}
spec:
  selector: {{- include "push.runtimeLabels" . | nindent 4 }}
  ports:
    - { name: http, port: {{ .Values.service.port }}, targetPort: public }
