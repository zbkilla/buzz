import type { FeatureDefinition } from "@/shared/features/types";

/**
 * Protected feature definitions compiled into the official OSS application.
 * Keep this module free of protected product names, metadata, and imports.
 */
export const protectedFeatureDefinitions: FeatureDefinition[] = [];
